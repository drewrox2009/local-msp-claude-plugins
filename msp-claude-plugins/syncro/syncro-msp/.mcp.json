{
  "mcpServers": {
    "syncro": {
      "type": "http",
      "url": "${SYNCRO_MCP_URL}",
      "headers": {
        "X-Syncro-API-Key": "${SYNCRO_API_KEY}",
        "X-Syncro-Subdomain": "${SYNCRO_SUBDOMAIN}"
      }
    }
  }
}
