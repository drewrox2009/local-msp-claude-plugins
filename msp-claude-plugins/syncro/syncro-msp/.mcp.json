{
  "mcpServers": {
    "syncro": {
      "command": "npx",
      "args": ["-y", "file:../../mcp-servers/syncro-mcp"],
      "env": {
        "SYNCRO_API_KEY": "${SYNCRO_API_KEY}",
        "SYNCRO_SUBDOMAIN": "${SYNCRO_SUBDOMAIN}"
      }
    }
  }
}
