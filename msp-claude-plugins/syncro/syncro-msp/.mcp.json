{
  "mcpServers": {
    "syncro": {
      "command": "npx",
      "args": ["-y", "github:wyre-technology/syncro-mcp"],
      "env": {
        "SYNCRO_API_KEY": "${SYNCRO_API_KEY}",
        "SYNCRO_SUBDOMAIN": "${SYNCRO_SUBDOMAIN}"
      }
    }
  }
}
