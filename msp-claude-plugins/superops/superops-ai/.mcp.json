{
  "mcpServers": {
    "superops": {
      "command": "npx",
      "args": ["-y", "github:wyre-technology/superops-mcp"],
      "env": {
        "SUPEROPS_API_TOKEN": "${SUPEROPS_API_TOKEN}",
        "SUPEROPS_SUBDOMAIN": "${SUPEROPS_SUBDOMAIN}"
      }
    }
  }
}
