{
  "mcpServers": {
    "huntress": {
      "type": "http",
      "url": "${HUNTRESS_MCP_URL}",
      "headers": {
        "X-Huntress-API-Key": "${HUNTRESS_API_KEY}",
        "X-Huntress-API-Secret": "${HUNTRESS_API_SECRET}"
      }
    }
  }
}
