{
  "mcpServers": {
    "huntress": {
      "command": "npx",
      "args": ["-y", "github:wyre-technology/huntress-mcp"],
      "env": {
        "HUNTRESS_API_KEY": "${HUNTRESS_API_KEY}",
        "HUNTRESS_API_SECRET": "${HUNTRESS_API_SECRET}"
      }
    }
  }
}
