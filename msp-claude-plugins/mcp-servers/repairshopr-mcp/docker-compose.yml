services:
  repairshopr-mcp:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    image: repairshopr-mcp:latest
    container_name: repairshopr-mcp-server
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      - REPAIRSHOPR_API_KEY=${REPAIRSHOPR_API_KEY}
      - REPAIRSHOPR_SUBDOMAIN=${REPAIRSHOPR_SUBDOMAIN:-}
      - MCP_TRANSPORT=http
      - AUTH_MODE=env
      - LOG_LEVEL=${LOG_LEVEL:-info}
      - NODE_ENV=production
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

volumes: {}
